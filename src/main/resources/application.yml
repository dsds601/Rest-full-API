server:
  port: 8088

#
#logging:
#  level:
#    org.springframework: DEBUG

spring:
  message:
    basename: messages
  security:
    user:
      name: admin
      password: admin
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    show-sql: true
    defer-datasource-initialization: true

management:
  endpoint:
    web:
      exporsure:
        include: "*"



h2:
  console:
    enabled: true
